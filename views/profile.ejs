<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">

    <h1 class="text-center text-primary">Employee Profile</h1>

    <div class="card mx-auto mt-4" style="width: 24rem;">
        <% if (employee.profilePicture) { %>
            <img src="/uploads/<%= employee.profilePicture %>" class="card-img-top" alt="Profile Picture">
        <% } else { %>
            <img src="/uploads/default.png" class="card-img-top" alt="Default Profile">
        <% } %>
        <div class="card-body">
            <h5 class="card-title"><%= employee.name %></h5>
            <p class="card-text"><strong>Email:</strong> <%= employee.email %></p>
            <p class="card-text"><strong>Position:</strong> <%= employee.position %></p>
            <a href="/" class="btn btn-secondary">Back to Home</a>
        </div>
    </div>

</body>
</html>
